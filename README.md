### Start with docker

`docker-compose up`



Docker + PM2 + Nginx + Express.js + MongoDB + MySQL + Redis

- **PM2 monitor**
- login + regist (JWT)
- mongoose pagination
- mysql pagination + mysql migrate
